stages:
  - test

cache:
  paths:
    - node_modules/

test:
  stage: test
  image: beevelop/nodejs-python
  variables:
    DB_URL: "mongodb://mongo/nodejs-restify-db"
  services:
    - mongo
  script:
    - npm i
    - npm test